#!/bin/sh -x

env

if [ -z "$JETTY_URL" ]; then
    echo >&2 "ERROR: JETTY_URL has empty value!"
    exit 1
fi

mkdir $HOME/jetty
wget "$JETTY_URL" -O - | tar zxvf - -C $HOME/jetty
ls -l $HOME
ls -l $HOME/jetty
